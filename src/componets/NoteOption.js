import React, { Component } from 'react';

class NoteOption extends Component {
	render() {
		return (
			<React.Fragment>
				<button>Cancel</button>
				<button>Create</button>
			</React.Fragment>
		);
	}
}

export default NoteOption;
